Algoritmo "sucessor"
Var
m15, mult, ant:real


Inicio
escreval("insira o numero inicial")
leia(m15)

ant <- (m15 - 1);
mult <- (m15) * (m15+1);

escreval("O antecessor do numero e ", ant," e o sucessor multiplicado e",mult)


Fimalgoritmo